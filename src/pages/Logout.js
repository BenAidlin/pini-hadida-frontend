import { Button } from "@mui/material";

const Logout = () => {
    return (
        <div>
            <Button
                onClick={()=>{
                    
                }}
            >Log out</Button>
        </div>
    );    
}

export default Logout;